<form method="post" class="d-inline" action="{{ path('app_coach_delete', {'id': coach.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.id) }}">
    <button class="btn btn-danger rounded-0">Borrar <i class="bi bi-trash"></i></button>
</form>
